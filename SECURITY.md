# 安全政策

## 支持范围

安全修复优先应用于最新发布版本和默认分支。

## 报告漏洞

请不要在公开 Issue 中披露可能导致行程数据泄露、任意文件读写或导入校验绕过的问题。优先使用 GitHub Security Advisories 的“Report a vulnerability”私下报告。

报告应包含影响范围、复现步骤、受影响版本和可行的缓解建议。请勿附带真实个人行程文件；使用最小化的虚构样例即可。

## 安全边界

行迹不提供云端账户或网络同步。主要安全边界包括：

- 本地 JSON 文件访问权限。
- 浏览器站点数据隔离。
- 原生文件选择器与 WebView 消息桥。
- 导入文件的结构校验和替换确认。

对本机拥有完整系统权限的恶意软件不在项目威胁模型内。
